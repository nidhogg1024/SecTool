{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "SecTool 默认权限配置",
    "windows": ["main"],
    "permissions": [
        "core:default",
        "shell:allow-open"
    ]
}
