{
    "name": "sectool-adapter-web",
    "scripts": {
        "build": "pnpm exec tsup-node",
        "release": "node dist/release.js",
        "adapter-build": "pnpm run build",
        "platform-release": "pnpm run release"
    },
    "dependencies": {
        "sectool-adapter-base": "workspace:*"
    },
    "devDependencies": {
        "@types/node": "catalog:",
        "tsup": "catalog:",
        "typescript": "catalog:"
    }
}
